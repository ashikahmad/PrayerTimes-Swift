# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AKPrayerTime' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AKPrayerTime
  pod 'Eureka'

end

target 'PrayerKit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PrayerKit

  target 'PrayerKitTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
